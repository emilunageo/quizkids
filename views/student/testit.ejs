<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test it !</title>
</head>
<body>
    <% for (var i = 0; i < Quizes.length; i++) { %>
        <a href="/take-quiz?quizId=<%= Quizes[i].id %>"><%= Quizes[i].nombre %></a>
        <% console.log(Quizes) %>
    <% } %>
    
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        var quizLinks = document.querySelectorAll('a[data-quiz-id]');
      
        quizLinks.forEach(function(link) {
            link.addEventListener('click', function(event) {
                // No se llama a event.preventDefault(); para permitir la navegación
                var quizId = this.getAttribute('data-quiz-id');
                // Aquí puedes hacer lo que necesites con el ID del quiz, como enviarlo a la ruta "/take-quiz"
            });
        });
    });
    </script>
    

</body>
</html>